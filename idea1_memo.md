### A. 極座標変換の勾配不安定性と計算コスト

現在のPyTorch実装における以下の部分には、勾配計算上のリスクとパフォーマンス上の課題があります。

```python
r = torch.abs(state_c) + self.eps
theta = torch.angle(state_c)

```

* **課題:** `torch.angle` は超越関数であり計算コストが高いだけでなく、複素平面の原点付近（`state_c` が $0$ に近い状態）において勾配が NaN（Not a Number）や Inf になる致命的なリスクがあります。`r` に `eps` を足しても、`angle()` 自体の入力が0に近い場合の勾配爆発は防げません。
* **解決案（直交座標ベースの位相演算）:**
$\theta$ を明示的に求める代わりに、直交成分（$x = \text{real}, y = \text{imag}$）から直接 $\sin\theta, \cos\theta$ を算出することで、`torch.angle` の呼び出しを完全に回避できます。

$$r = \sqrt{x^2 + y^2 + \epsilon}$$


$$\cos\theta = x / r, \quad \sin\theta = y / r$$



これにより、超越関数を排除し、勾配を安定させ、Triton等でのハードウェア演算効率（乗算と加算への還元）を飛躍的に高めることができます。

### B. Tensor Coreの非活用と演算バウンド

* **課題:** TritonでBF16にパッキングしたとしても、複素数の乗算や回転演算は本質的に「要素ごとの演算（Element-wise）」や「小規模な行列演算」の連続になりやすく、現代のGPUが最も得意とするTensor Core（巨大な行列積）をフル活用できません。
* **解決案:** Time-Mixingにおける KV結合処理だけでなく、状態更新のベクトル演算自体をブロック行列として再構築し、可能な限り `torch.matmul`（あるいはそれに相当するTriton上のMMA命令）にマッピングできるアーキテクチャへのリファクタリングが、スループット向上の鍵になります。

### C. 表現の獲得メカニズム（実数から複素数への意味的マッピング）

* **課題:** C-LoRAによるZero-Shot移行時、初期状態では情報はすべて「振幅（実数）」に偏っています。学習を進める中で、どのタイミング・どのような勾配の力が働いて「位相」に文脈情報が移動・構造化されるのか、そのダイナミクスが不透明です。
* **解決案:** 単純な言語モデリング（Next token prediction）だけでなく、初期フェーズでは「長距離の依存関係の解決」や「相反する事実の区別」など、**干渉と共鳴（位相）が明確に有利に働くトイタスク**（例：Associative RecallやLong-context Passkey retrieval）を用いて、位相空間への表現の分化を意図的に促すカリキュラム学習が有効と考えられます。
